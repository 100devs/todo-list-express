<!DOCTYPE html> <!-- Telling the browser what type of document will be shown -->
<html lang="en"> <!-- Starting the HTML tag and saying that the language is English -->
<head> <!-- Starting the head tag to store metadata -->
    <meta charset="UTF-8"> <!-- Indicates what character set should be used -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!-- Sets appropriate compatibility for Microsoft browsers -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Sets the width of available viewing area to the width of the device screen -->
    <title>Document</title> <!-- This is the text that appears inside the browser tab -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!-- creates a link to the Font Awesome library to use later -->
    <link rel="stylesheet" href="css/style.css"> <!-- Create a link to the local stylesheet in the public folder-->
</head> <!-- closing the head tag-->
<body> <!-- main section for the client to render/displayed -->
    <h1>Todo List: </h1> <!-- This is the header for the site -->
    <ul class="todoItems"> <!-- This is the start of the unordered list and assign the class to the list, for the todo list to populate under --> 
    <% for(let i=0; i < items.length; i++) {%> <!-- This is EJS this allows us to run a for loop for the amount of items in the db -->
        <li class="item"> <!-- This is going to render an list item  with class of item for each item in the db.   -->
            <% if(items[i].completed === true) {%> <!-- if the item in the array has a property of completed it would then run the condition below  -->
                <span class='completed'><%= items[i].thing %></span> <!-- This is the code to run if the item is completed. This will add the strikethrough css -->
            <% }else{ %> <!-- this is the second part of the conditional to run if the above is not true  -->
                <span><%= items[i].thing %></span> <!-- This is the code to run if the completed property is false -->
            <% } %>    <!-- This is the end of if/else condition in EJS -->
            <span class='fa fa-trash'></span> <!-- This is a span element that contains the font awesome image for a trash bin -->
        </li> <!-- This is the closing tag for the list item element -->
    <% } %> <!-- This is the closing tag for EJS -->
    </ul> <!-- This is the closing tag for the unordered list -->

    <h2>Left to do: <%= left %></h2> <!-- Create a smaller header tag. Also add some EJS to display a variable showing how many tasks are left to complete -->

    <h2>Add A Todo:</h2> <!-- Create a another header -->

    <form action="/addTodo" method="POST"> <!-- Create a form with a POST method that goes to the /addTodo route -->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!-- Create an input that accepts text. This is where the value for new todos will be added. There is a placeholder inside of it before you type anything -->
        <input type="submit"> <!-- Create an input button to submit the form  -->
    </form> <!-- Closing tag for the form -->
    

    <script src='js/main.js'></script> <!-- Script tag that links the main.js file in the public folder to run the client side javaScript -->
</body> <!-- body closing tag -->
</html> <!-- specifies the end of the html doc -->
