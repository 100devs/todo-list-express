<!DOCTYPE html> <!--Tells the browser what type of document it is showing-->
<html lang="en"> <!--Start of HTML tag and language is set to english-->
<head> <!--Starting head tag to store all out metadata-->
    <meta charset="UTF-8"> <!--Indicates what character set should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!--Sets appropriate compatibility for Microsoft browers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!--Sets width of available viewing to device width screen-->
    <title>Document</title> <!--Displays text on browser tab-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!--Links to stylesheet that allows use of icon library from font awesome-->
    <link rel="stylesheet" href="css/style.css"> <!--Links to stylesheet that uses the CSS file in css folder at public folder -->
</head> <!--Closes the head tag-->
<body> <!-- main section for content being displayed  -->
    <!DOCTYPE html>
<html lang="en">
<head> <!--Starting head tag to store all out metadata-->
    <meta charset="UTF-8"> <!--Indicates what character set should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">  <!--Sets appropriate compatibility for Microsoft browers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title> <!--Displays text on browser tab-->
</head> <!--Closes the head tag-->
<body> <!-- main section for content being displayed  -->
    <h1>Todo List: </h1> <!-- main title displayed on page  -->
    <ul class="todoItems"> <!--Create unordered list and assign class to the list-->
    <% for(let i=0; i < items.length; i++) {%> <!-- Use EJS to start a FOR loop-->
        <li class="item"> <!--Create a list item with a class of item-->
            <% if(items[i].completed === true) {%> <!--EJS checking if item in an array is a completed property as true-->
                <span class='completed'><%= items[i].thing %></span> <!--Creating an inline element with a class and adding EJS to display the name of the task-->
            <% }else{ %> <!--EJS for the Else clause of our if statement-->
                <span><%= items[i].thing %></span> <!--Added EJS to display the name of the task with NO CLASS-->
            <% } %>    <!--EJS to close the Else statement-->
            <span class='fa fa-trash'></span> <!--Display Font Awesome Icon of trash can inside a span-->
        </li> <!--Closing out List Item-->
    <% } %> <!--EJS to close the FOR loop-->
    </ul> <!--Closing our unordered list-->

    <h2>Left to do: <%= left %></h2> <!--Creates second lvl heading tag with text and number of things left to do using EJS, show how many tasks are left to complete-->

    <h2>Add A Todo:</h2> <!--Creates another second level header-->

    <form action="/addTodo" method="POST"> <!--Creates a form with a POST method that goes to /addTodo route-->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!--Adds an input box that accepts text to create additional tasks to the to do list-->
        <input type="submit"> <!--Adds a button to submit the task to the to do list-->
    </form> <!--Closing the form tag-->
    

    <script src='js/main.js'></script> <!--Links js file using script tag, js file located in js folder at public folder-->
</body> <!--Closing the body tag-->
</html> <!--Closing the html tag-->
