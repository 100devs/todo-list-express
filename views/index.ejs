<!DOCTYPE html> <!--Telling the browser what kind of document will be shown-->
<html lang="en"> <!--Start the HTML tag and saying the language is English-->
<head> <!--Start head tag to store our metadata-->
    <meta charset="UTF-8"> <!--indicates what characterset should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!--sets appropriate compatibility for Microsoft browsers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!--sets the width of available viewing area to the width of the device screen-->
    <title>Document</title> <!--assign title that appears on the browser tab-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!--link to fontawesome library-->
    <link rel="stylesheet" href="css/style.css"> <!--create link to the local stylesheet in the public folder-->
</head> <!--close head tag-->

<body> <!-- main section for content displayed-->
    <h1>Todo List: </h1> <!--Main title displayed on page-->
    <ul class="todoItems"> <!--Create unordered list and assign class to the list-->
    <% for(let i=0; i < items.length; i++) {%> <!--use EJS to start a for loop-->
        <li class="item"> <!--create a list item with class item-->
            <% if(items[i].completed === true) {%> <!--EJS to check if an item in an array with a completed property is true-->
               <span class='completed'><%= items[i].thing %></span> <!--create an inline element with a class and adding EJS to display the name of the task -->
            <% }else{ %> <!--EJS for the else clause of out if statement-->
                <span><%= items[i].thing %></span> <!-- create an inline element without a class and adding EJS to display the name of the task -->
            <% } %>    
            <span class='fa fa-trash'></span> <!--display Font Awesome Icon of trashcan inside span-->
        </li> <!--close list item-->
    <% } %> <!--EJS to close FOR loop-->
    </ul> <!--close unordered list-->

    <h2>Left to do: <%= left %></h2> <!-- Create a heading with text and uses EJS to count and display the number of incomplete tasks-->

    <h2>Add A Todo:</h2> <!--Create a second level header-->

    <form action="/addTodo" method="POST"> <!--create a form with a POST method that foes to the /addTodo route-->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!--create and input box that accepts text with default text inside of it before you type anything. Given a name for later reference-->
        <input type="submit"> <!--give the form a submit button-->
    </form> <!--close the form tag-->
    

    <script src='js/main.js'></script> <!--link to the main.js file in the js folder within the public folder-->
</body> <!--close the body tag-->
</html> <!--close the html-->
