<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body> 
    <h1>Todo List: </h1>
    <ul class="todoItems">
        <!-- EJS starts below, not commenting above as its super basic -->
    <% for(let i=0; i < items.length; i++) {%>
        <!-- creates a list item for each element in the item arrat -->
        <li class="item">
            <% if(items[i].completed === true) {%>
                <!-- if the item has the completed property, give it the completed class -->
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %> <!-- if the item doesnt have the completed property, create it normally -->
                <span><%= items[i].thing %></span>
            <% } %>    
            <span class='fa fa-trash'></span> <!-- gives the trash can icon for deletion -->
        </li>
    <% } %>
    </ul>

    <h2>Left to do: <%= left %></h2> <!-- adds the number of left to do items -->

    <h2>Add A Todo:</h2>

    <form action="/addTodo" method="POST"> <!-- adds the form for the user to put their to do items -->
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
