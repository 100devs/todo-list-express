<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Todo List: </h1>
    <ul class="todoItems"> <!-- This ejs "deletes" client side, but keeps info in DB-->
    <% for(let i=0; i < items.length; i++) {%>   <!-- loop through items -->    
            <% if(items[i].deleteClientSide === true) {%> <!--- if item deletCli.. is true then-->
                <li class="item deleted"> <!--- add delted to item li -->
                <span class='deleted'><%= items[i].thing %></span>   <!--- add .deleted to span-->             
                <span class='fa fa-trash deleted'></span>           <!--- add .deleted to trashcan-->
                </li>                                               <!--- Close li---  Do this for completed/ uncompleted as well. Creating a new li for each posibility-->
            <% }else if(items[i].completed === true) {%>
                <li class="item">
                <span class='completed'><%= items[i].thing %></span>
                <span class='fa fa-trash'></span>
                </li>
            <% }else{ %>
                <li class="item">
                <span><%= items[i].thing %></span>
                <span class='fa fa-trash'></span>
                </li>
            <% } %>              
        
    <% } %>
    </ul>

    <h2>Left to do: <%= left %></h2> <!--- left comes from server.js main path response render-->

    <h2>Add A Todo:</h2>

    <form action="/addTodo" method="POST">
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
