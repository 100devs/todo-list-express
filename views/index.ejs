<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>

    <body>
        <h1>Todo List: </h1>
        <ul class="todoItems">
            <!-- creates list with 'items', items coming from server.js 'response.render' -->
            <% for(let i=0; i < items.length; i++) {%>
                <li class="item">
                    <!-- checks if property 'completed' of each item is true -->
                    <% if(items[i].completed===true) {%>
                        <!-- creates span with item.thing with class 'completed' -->
                        <span class='completed'>
                            <%= items[i].thing %>
                        </span>

                        <% }else{ %>
                            <!-- if condition not true then creates span without class -->
                            <span>
                                <%= items[i].thing %>
                            </span>
                            <% } %>
                                <!-- creates trashbin icons for each line -->
                                <span class='fa fa-trash'></span>
                </li>
                <% } %>
        </ul>
        <!-- 'left' comes from app.get server.js and it's the counter with documents having property 'completed' as false -->
        <h2>Left to do: <%= left %>
        </h2>

        <h2>Add A Todo:</h2>
        <!-- form that goes directly to server.js creating a post method -->
        <form action="/addTodo" method="POST">
            <!-- name="todoItem" will be requested by server.js in order to determine what to use for the post method -->
            <input type="text" placeholder="Thing To Do" name="todoItem">
            <input type="submit">
        </form>


        <script src='js/main.js'></script>
    </body>

    </html>