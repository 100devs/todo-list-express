// this tells the browser what version of HTML is being used which helps the
browser run
<!DOCTYPE html>
// specifies language is english, useful for search engines and assist
technologies
<html lang="en">
  <head>
    //sets the character encoding to support a wide variety of languages and
    scripts
    <meta charset="UTF-8" />
    //sets the compatibility for internet explorer

    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    //scales the viewport of the webpage to the width of the screen
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    // sets the title of the webpage
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
    //links the html to the style.css stylesheet
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    //h1 for page
    <h1>Todo List:</h1>
    // ul with class for items
    <ul class="todoItems">
      // ejs for loop for all looping through all the items <% for(let i=0; i <
      items.length; i++) {%> //li for adding to the ul
      <li class="item">
        // checks if the completed property in items is equal to true if it is
        it will run the code <% if(items[i].completed === true) {%> // the thing
        value in the items object is dynamically generated using ejs templating
        <span class="completed"><%= items[i].thing %></span>
        // ejs else statement <% }else{ %> // ejs templating generates the value
        of the items object on the page in a span
        <span><%= items[i].thing %></span>
        //ejs closing bracket <% } %> // font awesome trash icon
        <span class="fa fa-trash"></span>
      </li>
      // ejs closing bracket <% } %>
    </ul>
    // left value is dynamically generated by ejs in an h2
    <h2>Left to do: <%= left %></h2>
    // standard h2
    <h2>Add A Todo:</h2>
    // form data is sent to the /addTodo URL has a POST method sending the form
    data in the body of the HTTP request
    <form action="/addTodo" method="POST">
      // text input for form
      <input type="text" placeholder="Thing To Do" name="todoItem" />
      // submit button for form
      <input type="submit" />
    </form>
    // linking main.js to index.ejs
    <script src="js/main.js"></script>
  </body>
</html>
