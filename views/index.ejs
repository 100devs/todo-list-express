<!DOCTYPE html>  <!--Telling browser what type of document will be shown. -->
<html lang="en"> <!-- Starting the HTML tag and Saying that the language is english-->
<head>  <!--Starting the header tag to store our meta data.-->
    <meta charset="UTF-8">  <!--Indicates what character set should be used.-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">  <!--Sets appropriate compatibility of Microsoft browsers.-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">  <!--Sets the Width of the avaliable viewing area to the width of
    the device screen.-->
    <title>Todo List.</title> <!--Naming the browser tab.-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!--Creates link to the fon't Awe
    some library-->
    <link rel="stylesheet" href="css/style.css"> <!-- Linking local stylesheet.-->
</head> <!--Closing the header tag-->
<body> <!-- Main section for content displayed. -->
    <h1>Todo List: </h1>  <!-- Main title displayed on page -->
    <ul class="todoItems">  <!-- Create unordered list and assign a class to the list. -->
    <% for(let i=0; i < items.length; i++) {%>  <!--Using EJS to start a FOR  loop-->
        <li class="item">  <!--Creating a list item-->
            <% if(items[i].completed === true) {%><!--EJS to check if an item in an array with the completed property is true-->
                <span class='completed'><%= items[i].thing %></span> <!-- Creating an inline element with a class and adding EJS to display 
                the name of the task.-->
            <% }else{ %> <!-- EJS for the Else clause of our if statement. -->
                <span><%= items[i].thing %></span>  <!-- Adding EJS to display the name of the task with NO CLASS -->
            <% } %> <!--EJS to close the else statement. -->
            <span class='fa fa-trash'></span>  <!-- Display Font awesome Icon of trashcan inside a span-->
        </li> <!-- Closing our list Item. -->
    <% } %>  <!-- EJS to close the FOR Loop -->
    </ul>  <!--Closing unordered list.-->

    <h2>Left to do: <%= left %></h2> <!--Creates second level heading tag with text and using EJS to display a variable showing how many tasks
    are left to complete. -->

    <h2>Add A Todo:</h2>  <!-- Create a second level header  -->

    <form action="/addTodo" method="POST">  <!--Creating a for with a POST method that goes to the todo /addTodo route. -->
        <input type="text" placeholder="Thing To Do" name="todoItem">  <!-- Creating input box that accepts text with default text
        inside of it before you type anything. The input box is given a name for future reference.-->
        <input type="submit">  <!-- gives the form a submit button-->
    </form>  <!--Closing form tag -->
    

    <script src='js/main.js'></script>  <!-- Linking main.js folder in the public folder.-->
</body>  <!-- Closing the body tag -->
</html> <!--Closing the html tag-->
