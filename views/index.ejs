<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

</head>
<body>
    <h1>Todo List: </h1> <!-- Header that  -->
    <ul class="todoItems">
        <!-- Run a for loop as many times as the items inside 'items' -->
    <% for(let i=0; i < items.length; i++) {%>
        <li class="item">
            <!-- Create an li for each iteration , with class 'item' -->
            <!-- if the item is completed , create a paragraph with class of 'completed' -->
            <% if(items[i].completed === true) {%>
                <p class='completed'><%= items[i].thing %></p>
            <% }else{ %>
                <!-- Else, just create the item with no class  -->
                <p><%= items[i].thing %></p>
            <% } %> 
               <!-- Create a span that is is actually a fontawesome icon of a trash can -->
            <span class='fa fa-trash'></span>
        </li>
    <% } %>
    </ul>

    <!-- Header showing how many items are left todo -->
    <h2>Left to do: <%= left %></h2> 

    <h2>Add A Todo:</h2>
    <!-- Form that sends a POST request to the route /addTodo -->
    <form action="/addTodo" method="POST">
        <!-- input field that takes the todo itam, and sends it to the server under the name of "todoItem" -->
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <!-- simple submit input -->
        <input type="submit">
    </form>
    
    <!-- Script tag that links main.js file to ejs file -->
    <script src='js/main.js'></script>
</body>
</html>
