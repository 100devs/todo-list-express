<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todo List: </h1>
    <ul class="todoItems">
    <!-- looping through our list of items -->
    <% for(let i=0; i < items.length; i++) {%>
        <li class="item">
            <!-- if these items are completed, they'll be given a class of 'completed' and styled to have a line through it -->
            <% if(items[i].completed === true) {%>
                <p class='completed'><%= items[i].thing %></p>
            <% }else{ %>
                <!-- else create the item without the class -->
                <p><%= items[i].thing %></p>
            <% } %>
            <!-- Create a span with a font awesome icon of a trash can -->
            <span class='fa fa-trash'></span>
        </li>
    <% } %>
    </ul>

    <h2>Left to do: <%= left %></h2>

    <h2>Add A Todo:</h2>

    <form action="/addTodo" method="POST">
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
