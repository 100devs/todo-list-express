<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
      </head>
      <body>
        <!-- The main heading of the page -->
        <h1>Todo List:</h1>
        <!-- The list of todo items -->
        <ul class="todoItems">
          <!-- Loop through the length of the items array -->
          <% for(let i=0; i < items.length; i++) {%>
          <li class="item">
            <!-- If the item at position (index) is completed -->
            <% if(items[i].completed === true) {%>
            <span class="completed"><%= items[i].thing %></span>
            <% }else{ %>
            <span><%= items[i].thing %></span>
            <% } %>
            <span class="fa fa-trash"></span>
          </li>
          <% } %>
        </ul>
        <!-- Todo items that haven't been completed -->
        <h2>Left to do: <%= left %></h2>

        <h2>Add A Todo:</h2>
        <!-- This form will send data to our server via POST method -->
        <form action="/addTodo" method="POST">
          <!-- Text input that let's the user enter their todo item -->
          <input type="text" placeholder="Thing To Do" name="todoItem" />
          <input type="submit" />
        </form>

        <script src="js/main.js"></script>
      </body>
    </html>
  </body>
</html>
