<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
      </head>
      <body>
        <h1>Todo List:</h1>
        <ul class="todoItems">
          <!-- for loop iterates over array of todo items and creates li elements for each item. -->
          <% for(let i=0; i < items.length; i++) {%>
          <li class="item">
            <!-- IF current item's property completed is true, inside li element it creates span with class .completed-->
            <% if(items[i].completed === true) {%>
            <span class="completed"><%= items[i].thing %></span>
            <!-- otherwise it creates a regular span inside li element -->
            <% }else{ %>
            <span><%= items[i].thing %></span>
            <% } %>
            <!-- then it creates span with classes .fa and .fa-trash (trash can) inside li element -->
            <span class="fa fa-trash"></span>
          </li>
          <% } %>
        </ul>
        <!-- displays variable left which contains value from the server of how many todo items left-->
        <h2>Left to do: <%= left %></h2>

        <h2>Add A Todo:</h2>

        <!-- a form to send post request to API -->
        <form action="/addTodo" method="POST">
          <input type="text" placeholder="Thing To Do" name="todoItem" />
          <input type="submit" />
        </form>

        <script src="js/main.js"></script>
      </body>
    </html>
  </body>
</html>
