<!DOCTYPE html> <!-- tells browser what type of document will be shown --> <!-- index.ejs renders html with embedded javascript -->
<html lang="en"> <!-- html starting tag and specifying language as English -->
<head> <!-- starting tag for head for metadata -->
    <meta charset="UTF-8"> <!-- indicates what character set should be used -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!-- sets appropriate compatibility for Microsoft browsers -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- sets the width of available viewing area to the width of the device screen -->
    <title>Document</title> <!-- title tag names the tab in browser -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!-- links to font awesome icon library -->
    <link rel="stylesheet" href="css/style.css"> <!-- links to css file in css folder in public folder -->
</head> <!-- closing tag for head -->
<body> <!-- main section for content being displayed -->
    <!DOCTYPE html> <!-- another head section? typo? -->
<html lang="en"> <!-- another head section? typo? -->
<head> <!-- another head section? typo? -->
    <meta charset="UTF-8"> <!-- another head section? typo? -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!-- another head section? typo? -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- another head section? typo? -->
    <title>Document</title> <!-- another head section? typo? -->
</head> <!-- another head section? typo? -->
<body> <!-- main section for content being displayed -->
    <h1>Todo List: </h1> <!-- main header displayed on page -->
    <ul class="todoItems"> <!-- unordered list with a class of "todoItems" -->
    <% for(let i=0; i < items.length; i++) {%> <!-- ejs FOR loop that will run through array of objects from database -->
        <li class="item"> <!-- list item with a class of "item" -->
            <% if(items[i].completed === true) {%> <!-- ejs conditional to see if task (item in an array) is completed (true) -->
                <span class='completed'><%= items[i].thing %></span> <!-- span with a class of "completed" and ejs to display task (from database with property of thing)-->
            <% }else{ %> <!-- ejs for else clause of if statement -->
                <span><%= items[i].thing %></span> <!-- ejs putting task name in span with no class specified -->
            <% } %> <!-- ejs closing else statement -->
            <span class='fa fa-trash'></span> <!-- span with class of trash can icon using font awesome -->
        </li> <!-- closing tag for list item -->
    <% } %> <!-- ejs closing for loop -->
    </ul> <!-- closing tag for unordered list -->

    <h2>Left to do: <%= left %></h2> <!-- second level header displaying some text and using ejs to display a variable showing number of tasks left to do -->

    <h2>Add A Todo:</h2> <!-- second level header displaying some text -->

    <form action="/addTodo" method="POST"> <!-- creating a form with a POST method that goes to the /addTodo route -->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!-- input tag creates an inpute box that allows for text; placeholder is default text inside box; name for input box to easily reference it-->
        <input type="submit"> <!-- gives the form a submit button -->
    </form> <!-- closing tag for form -->
    

    <script src='js/main.js'></script> <!-- link to javascript file located in js folder in public folder -->
</body> <!-- closing tag for body -->
</html> <!-- closing tag for html -->
