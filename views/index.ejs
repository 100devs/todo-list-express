

 <!--This page is used to create the ui for the main page of our app-->
<!---It is an EJS file which allows us to embed JavaScript with in html-->
<!DOCTYPE html><!--Letting the browser know what type of document will be shown-->
<html lang="en"><!--Stating that the language will be english and starting the html tag-->
<head><!--Starting the head tag, which stored the metadata-->
    <meta charset="UTF-8"><!--Declares the coding type for the characters that should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"><!--Sets appropriate compatibility for Microsoft browsers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"><!--Sets the width of the available viewing area to the width of the device screen-->
    <title>Document</title><!--Giving the title that will be displayed on the browser tag-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"><!--linking to font-awsome icon library-->
    <link rel="stylesheet" href="css/style.css"><!--linking to the local stylesheet that is in the css folder within the public folder.-->
</head><!--Closing the head tag-->
<body><!--This could be a typo-->
    
    <!DOCTYPE html><!--This could be a typo-->
<html lang="en"><!--This could be a typo-->
<head><!--This could be a typo-->
    <meta charset="UTF-8"><!--This could be a typo-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"><!--This could be a typo-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"><!--This could be a typo-->
    <title>Document</title><!--This could be a typo-->
</head><!--This could be a typo-->
<body>
    <h1>Todo List: </h1><!--Creating the main heading on the page-->
    <ul class="todoItems"><!--Used to create an unordered list with a class assigned to it-->
    <% for(let i=0; i < items.length; i++) {%> <!--Use EJS to start a for loop-->
        <li class="item"> <!--Creating a list item with a class of 'item'-->
            <% if(items[i].completed === true) {%> <!--EJS, Checking to see if a particular item(task) in the array is true (is it completed)-->
                <span class='completed'><%= items[i].thing %></span><!--Creating a span with a class 'completed', using EJS to display the name of the task-->
            <% }else{ %> <!--Ejs for the else claust of the if statement-->
                <span><%= items[i].thing %></span> <!--Adding EJS to display the name of the task with no class -->
            <% } %>    <!--EJS to close the else statement-->
            <span class='fa fa-trash'></span><!--Using fontawsome icon to create a trashcan inside a span-->
        </li><!--closing the list item-->
    <% } %><!--closing EJS fot the For loop-->
    </ul><!--Closing the unordered list-->

    <h2>Left to do: <%= left %></h2><!--Creating a heading second level tag with text and using EJS to display a variable showing how many tasks are remaining -->

    <h2>Add A Todo:</h2><!--Creating a second level header-->

    <form action="/addTodo" method="POST"><!--Creating a form with a post method that add whatever is typed in to the route '/addTodo' POST = Create. The route is added so that the server knows where to go to add.-->
        <input type="text" placeholder="Thing To Do" name="todoItem"><!--Creating an input box, specifing that the input should be text. The placeholder shows when the user types something.The input box is assigned a unique name so that it can be referenced later-->
        <input type="submit"><!--Gives the form a submit button-->
    </form><!--Closing the form -->


    <script src='js/main.js'></script><!--Linking to a file called main.js that in js folder within the public folder-->
</body><!--Closing the body tag-->
</html><!--Closing the html tag-->
