<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Todo List: </h1>
    <ul class="todoItems">
    <% for(let i=0; i < items.length; i++) {%> <!--setting the loop-->
        <li class="item">
            <!--checking if the item is complited -->
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span> 
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>    
            <!-- adding a trash can -->
            <span class='fa fa-trash'></span>
        </li> g
          
    <% } %>
    </ul>

  <h2>Left to do: <%= left %></h2>    <!--how many todos are left uncomplited  -->

    <h2>Add A Todo:</h2>

    <!-- form for adding new items  -->
    <form action="/addTodo" method="POST">
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!--using name to know what form should go into backend  -->
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
