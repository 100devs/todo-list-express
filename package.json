{
  "name": "rap-name-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "comments": "echo \"JSON does not permit comments, but here's a small one I have hidden in the scripts. Why is our main point of entry listed as index.js when we have server.js? It still seems to work regardless.\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "mongodb": "^3.6.5"
  }
}
