<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <!-- traditional html heading-->

    <body>
        <h1>Todo List: </h1>
        <!--most important heading-->
        <ul class="todoItems">
            <% for(let i=0; i < items.length; i++) {%>
                <!--Another list item is added when the page reloads-->
                <li class="item">
                    <% if(items[i].completed===true) {%>
                        <span class='completed'>
                            <%= items[i].thing %>
                        </span>
                        <!--if the item is marked complete then respond with the information from the thing object with the class complete styles it from css in gray to show completed-->
                        <% }else{ %>
                            <span>
                                <%= items[i].thing %>
                            </span>
                            <% } %>
                                <span class='fa fa-trash'></span>
                                <!--creates a span holding the trash icon-->
                </li>
                <% } %>
        </ul>

        <h2>Left to do: <%= left %>
                <!--items not completed will be rendered based on the object created with the get method where a variable holds the incomplete items-->
        </h2>

        <h2>Add A Todo:</h2>

        <form action="/addTodo" method="POST">
            <input type="text" placeholder="Thing To Do" name="todoItem">
            <!--form that adds textbox to input to do list items with the create method in crud-->
            <input type="submit">
        </form>


        <script src='js/main.js'></script>
    </body>

    </html>